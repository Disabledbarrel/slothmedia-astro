---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout>
	<main class="container">
		<section class="landing">
			<div class="slogan">
				<div class="slogan-container">
					<p>Connect</p>
					<p>Your</p>
					<p>Favorite</p>
					<p>Music <i class="fa-solid fa-circle"></i></p>
				</div>
			</div>
			<img
				class="concept-img"
				src="/assets/sloth-music.png"
				alt="sloth listening to music"
			/>
		</section>
		<section class="information">
			<h3>Why Slothmedia?</h3>
			<div class="information-container-one">
				<h4>
					<i class="fas fa-play-circle"></i>
					Create your own playlists
				</h4>
				<p>Use music from many sources</p>
			</div>
			<div class="information-container-two">
				<h4>
					<i class="fas fa-headphones"></i>
					No commercial breaks
				</h4>
				<p>Just enjoy the music</p>
			</div>
		</section>
		<section class="concept">
			<h3>How it works</h3>
			<p>
				We built this place for the professional procrastinators, the couch
				champions, and anyone who just wants their favorite tracks in one cozy
				corner. Create an account, throw your songs into a playlist (YouTube
				links, whatever you’ve got), hit play — and that’s it. Just you, your
				music, and maximum chill. Your music the lazy sloth way.
			</p>
			<a
				href="about.html"
				class="btn btn-dark"
				title="Read more about how Slothmedia works">Read more</a
			>
		</section>
	</main>
</BaseLayout>
<style>
	.landing {
		display: grid;
		grid-template-columns: 1fr 300px;
	}
	.slogan {
		position: relative;
	}
	.slogan-container {
		margin: 0;
		position: absolute;
		top: 50%;
		-ms-transform: translateY(-50%);
		transform: translateY(-50%);
		font-size: 32px;
		font-weight: bold;
	}
	.slogan-container i {
		color: var(--color-accent);
		margin-left: var(--space-xs);
	}
	.landing img {
		grid-column: 2;
	}
	.information {
		display: flex;
		flex-direction: column;
		gap: var(--space-md);
		background: var(--color-primary);
		text-align: center;
		padding: var(--space-md);
		margin-bottom: var(--space-md);
	}

	.information i {
		margin-right: var(--space-xs);
	}

	.concept {
		padding: var(--space-md);
	}

	/* Mobile */
	@media (max-width: 768px) {
		.landing {
			grid-template-columns: 1fr 200px;
		}
	}
</style>
